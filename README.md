## Savenum

Desktop app for saving files to target directory enumerated with n amount of digits by dragging them into the app.
Increment the number and fill gaps automatically.
Developed using Wails.
